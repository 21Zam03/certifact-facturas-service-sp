<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.certicom.certifact_facturas_service_sp.mapper.TypeFieldMapper">
    <select id="getIdByName" parameterType="String">
        SELECT id FROM type_field WHERE name = #{valor}
    </select>
</mapper>